name: iOS CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js environment
        uses: actions/setup-node@v6.5.0
        with:
          node-version: '18.x'

      - name: Install dependencies
        run: npm ci

      - name: Run tests / build
        run: |
          # Replace these placeholders with your actual iOS build/test steps
          echo "Run iOS build or tests here"
